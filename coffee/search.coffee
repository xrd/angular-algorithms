@app.controller 'BinarySearch', [ '$scope', ($scope) ->

        $scope.properties = {
                notes: [
                        "Binary search is O(log n) algorithm"
                        ]
                complexity: 'O(log n)'
                }

        ]