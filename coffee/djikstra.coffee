@app.controller 'DjikstraCtrl', [ '$scope', 'Algorithm', ( $scope, Algorithm ) ->

        $scope.init = ->

                console.log "Inside the init"

        $scope.start = ->
                

        ]